# Using the following command gives us the following result

# net12=$(eval ifconfig | grep 'broadcast' | awk -F'inet ' '{print $2}' | awk -F' ' '{print $1}')
# dig +short -x $net12

#result
e1z2r9p13.42.us.org.

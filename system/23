# To connect as the new user, use the following command. Substitute 'username' with your login.
ssh username

# It is also possible to log in with normal/super-user privileges using su command.
su - username
